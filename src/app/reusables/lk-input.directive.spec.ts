import { LkInputDirective } from './lk-input.directive';

describe('LkInputDirective', () => {
  it('should create an instance', () => {
    const directive = new LkInputDirective({} as any);
    expect(directive).toBeTruthy();
  });
});
